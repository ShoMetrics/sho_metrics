# GitHub Repository Sample Source

Reference:

- https://docs.github.com/en/rest/repos/repos#get-a-repository

Sample provenance:

- `input.json` is a real response captured on 2026-06-08 from:
  `https://api.github.com/repos/steipete/CodexBar`
- The sample is trimmed to the scalar fields used by the POC. The official
  endpoint returns a larger object with the same fields.

Transform capabilities covered:

- no-auth public JSON API;
- simple scalar extraction;
- fixed labels and unit assignment;
- baseline case that should not require advanced transform features.

