# Custom Metric Action Helpers

`Custom Metric` names product-level target and rendering code.
`Custom HTTP` names the V1 source implementation behind that target.

Keep source registration, editor requests, and HTTP-derived widget data under
the Custom HTTP vocabulary. Keep product view assembly under Custom Metric
vocabulary.
