+++
title = "Does ShoMetrics need the helper?"
description = "When the optional Windows helper is needed and when it is not."
weight = 10
+++

Some metrics work directly in the plugin. Hardware-specific metrics may need the helper.

macOS users only need the Stream Deck plugin. On Windows, install the helper when you want deeper hardware sensor data.
